{
  "name": "@tiagoh/server-goat-defi-data",
  "version": "0.1.0",
  "description": "Flagship paid MCP server: GOAT/BTC market data, RWA prices, DeFi yields (live + labeled fallback).",
  "type": "module",
  "bin": {
    "tiagoh-goat-defi-data": "./dist/index.js"
  },
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@tiagoh/core": "workspace:*",
    "@modelcontextprotocol/sdk": "^1.12.0",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "tsup": "^8.3.5",
    "typescript": "^5.6.3"
  }
}
